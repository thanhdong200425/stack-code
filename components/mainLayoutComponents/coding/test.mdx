### Sum of Two Numbers

This problem requires you to add two integers and return their sum.

**Input Format:**  
Two integers `a` and `b` separated by a space.

**Output Format:**
A single integer representing the sum of `a` and `b`.

Example:

```javascript
a, (b = map(int, input().split()));
print(a + b);
```
